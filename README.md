# Client-and-Transaction-Management-System
This C++ console application manages clients, transactions, and basic user accounts. Data is stored in .txt files with simple encryption. The system includes a menu-driven interface and implements user permissions using binary numbers to control access to certain features.
